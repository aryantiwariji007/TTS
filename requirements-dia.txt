# Dia TTS backend dependencies (GPU/CUDA required)
#
# RTX 5000 series (Blackwell) requires torch 2.8 nightly — see INSTALL_DIA.md
# Install in two steps:
#
#   Step 1 (torch nightly for CUDA 12.8):
#     pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu128
#
#   Step 2 (remaining Dia deps):
#     pip install -r requirements-dia.txt
#
# For RTX 30/40 series (Ampere/Ada) use the stable torch instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-dia.txt

--extra-index-url https://download.pytorch.org/whl/nightly/cu128
--pre
torch>=2.8.0.dev0
dia @ git+https://github.com/nari-labs/dia.git
huggingface_hub>=0.23.0
